{{!-- Auto-generated grouped listing template for: report-general-ledger --}}
<!DOCTYPE html>
<html><head><meta charset="UTF-8">
<style>
{{{css}}}
.group-header td { border: none !important; }
.group-subheader td { border-bottom: 1px solid #e2e8f0 !important; }
</style>
</head>
<body><div class="report-container">
  <div class="report-header">
    <div><div class="report-title">{{meta.title}}</div></div>
    <div class="report-meta"><div>{{formatDate meta.generatedAt}}</div><div>{{meta.recordCount}} records</div></div>
  </div>

  {{#if meta.filters.length}}
  <div class="report-filters"><strong>Filters:</strong>
    {{#each meta.filters}}<span>{{this.label}}: {{this.value}} &nbsp;</span>{{/each}}
  </div>
  {{/if}}

  <table class="report-table">
    <thead><tr>
      <th style="width: 10%">Date</th>
      <th style="width: 25%">Business Partner</th>
      <th style="width: 15%">Debit</th>
      <th style="width: 15%">Credit</th>
      <th style="width: 15%">Balance</th>
    </tr></thead>
    <tbody>
      {{#each rows}}
        {{#if (isGroupBreak 'name' (lookup this 'name'))}}
        <tr class="group-header group-level-0">
          <td colspan="5" style="background: #1a1a2e; color: #ffffff; font-weight: 600; padding: 2mm 4mm; font-size: 11pt;">
            Account: {{lookup this 'name'}}
          </td>
        </tr>
        <tr class="group-subheader">
          <td colspan="5" style="background: #f1f5f9; padding: 1.5mm 4mm; font-size: 8pt; color: #475569;">
            <span style="margin-right: 3mm;"><strong>Code:</strong> {{lookup this 'value'}}</span>
          </td>
        </tr>
        {{/if}}
        <tr>
          <td>{{formatDate (lookup this 'dateacct')}}</td>
          <td>{{lookup this 'groupbyname'}}</td>
          <td class="cell-amount">{{formatCurrency (lookup this 'amtacctdr')}}</td>
          <td class="cell-amount">{{formatCurrency (lookup this 'amtacctcr')}}</td>
          <td class="cell-amount">{{formatCurrency (lookup this 'total')}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <div class="report-footer">
    <div class="report-summary">Total: {{meta.recordCount}} records</div>
    <div>Generated by Etendo Go</div>
  </div>
</div></body></html>
