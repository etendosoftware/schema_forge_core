import WarehousePickingListPage from './WarehousePickingListPage';

export default function App({ token, apiBaseUrl, window }) {
  return <WarehousePickingListPage token={token} apiBaseUrl={apiBaseUrl} window={window} />;
}
