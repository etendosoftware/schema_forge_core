import CommissionRunPage from './CommissionRunPage';

export default function App({ token, apiBaseUrl, window }) {
  return <CommissionRunPage token={token} apiBaseUrl={apiBaseUrl} window={window} />;
}
