{{!-- Auto-generated grouped listing template for: profit-loss --}}
<!DOCTYPE html>
<html><head><meta charset="UTF-8">
<style>
{{{css}}}
.group-header td { border: none !important; }
.group-subheader td { border-bottom: 1px solid #e2e8f0 !important; }
.report-totals { display: flex; gap: 8px; margin-top: 8mm; }
.total-card { flex: 1; border: 1px solid #e2e8f0; border-radius: 6px; padding: 4mm 5mm; background: #ffffff; }
.total-label { font-size: 7.5pt; font-weight: 700; color: #64748b; letter-spacing: 0.08em; text-transform: uppercase; margin-bottom: 1.5mm; }
.total-value { font-size: 18pt; font-weight: 700; color: #0f172a; }
</style>
</head>
<body><div class="report-container">
  <div class="report-header">
    <div><div class="report-title">{{meta.title}}</div></div>
    <div class="report-meta"><div>{{formatDate meta.generatedAt}}</div><div>{{meta.recordCount}} records</div></div>
  </div>

  {{#if meta.filters.length}}
  <div class="report-filters"><strong>Filters:</strong>
    {{#each meta.filters}}<span>{{this.label}}: {{this.value}} &nbsp;</span>{{/each}}
  </div>
  {{/if}}

  <table class="report-table">
    <thead><tr>
      <th style="width: 55%">Account</th>
      <th style="width: 25%">Amount</th>
    </tr></thead>
    <tbody>
      {{#each rows}}
        {{#if (isGroupBreak 'category' (lookup this 'category'))}}
        <tr class="group-header group-level-0">
          <td colspan="2" style="background: #1a1a2e; color: #ffffff; font-weight: 600; padding: 2mm 4mm; font-size: 11pt;">
            Category: {{lookup this 'category'}}
          </td>
        </tr>
        {{/if}}
        <tr>
          <td>{{lookup this 'account'}}</td>
          <td class="cell-amount">{{formatCurrency (lookup this 'amount')}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <div class="report-totals">
    <div class="total-card">
      <div class="total-label">Total Revenue</div>
      <div class="total-value">{{formatCurrency (sumRowsByCategory rows "1." "amount")}}</div>
    </div>
    <div class="total-card">
      <div class="total-label">Total Expenses</div>
      <div class="total-value">{{formatCurrency (sumRowsByCategory rows "2." "amount")}}</div>
    </div>
    <div class="total-card">
      <div class="total-label">Net Income</div>
      <div class="total-value">{{formatCurrency (sumRowsByCategory rows "3." "amount")}}</div>
    </div>
  </div>

  <div class="report-footer">
    <div class="report-summary">Total: {{meta.recordCount}} records</div>
    <div>Generated by Etendo Go</div>
  </div>
</div></body></html>
