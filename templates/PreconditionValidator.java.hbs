// GENERATED BY SCHEMA FORGE — DO NOT EDIT
// Generated: {{generatedDate}}
package {{package}};

import java.util.ArrayList;
import java.util.List;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.openbravo.base.exception.OBException;
{{#if entityFullClass}}
import {{entityFullClass}};
{{/if}}

/**
 * Precondition Validator: {{className}}
 *
 * Entity: {{entity}} ({{entityClassname}})
 * Preconditions: {{preconditions.length}}
 */
public class {{className}} {

  private static final Logger log = LogManager.getLogger();

{{#if entityFullClass}}
  /**
   * Validate all preconditions for the given entity.
   *
   * @param entity the {{entityClassname}} to validate
   * @throws OBException if any precondition fails
   */
  public void validate({{entityClassname}} entity) {
    final List<String> errors = new ArrayList<>();

{{#each preconditions}}
    // {{id}}: {{{assertion}}}
    // TODO(vertical-slice): implement precondition check
    // Expected error: {{{errorMessage}}}
{{/each}}

    if (!errors.isEmpty()) {
      throw new OBException(String.join("; ", errors));
    }
  }
{{else}}
  /**
   * Validate all preconditions (no OBDal class available for {{entity}}).
   */
  public void validate(org.openbravo.base.structure.BaseOBObject entity) {
    final List<String> errors = new ArrayList<>();

{{#each preconditions}}
    // {{id}}: {{{assertion}}}
    // TODO(vertical-slice): implement precondition check
    // Expected error: {{{errorMessage}}}
{{/each}}

    if (!errors.isEmpty()) {
      throw new OBException(String.join("; ", errors));
    }
  }
{{/if}}
}
