// GENERATED BY SCHEMA FORGE — DO NOT EDIT
// Generated: {{generatedDate}}
package {{package}};

import java.io.IOException;
import java.util.LinkedHashMap;
import java.util.Map;

/**
 * JSON Error Serializer: {{className}}
 *
 * Converts exceptions and error messages into a standard JSON error response.
 */
public class {{className}} {

  /**
   * Serialize an error message into a JSON-compatible map.
   *
   * @param errorCode the machine-readable error code
   * @param message the human-readable error message
   * @return a map representing the JSON error structure
   */
  public Map<String, Object> serialize(String errorCode, String message) {
    final Map<String, Object> error = new LinkedHashMap<>();
    error.put("error", true);
    error.put("code", errorCode);
    error.put("message", message);
    error.put("timestamp", System.currentTimeMillis());
    return error;
  }

  /**
   * Serialize an exception into a JSON-compatible map.
   *
   * @param errorCode the machine-readable error code
   * @param exception the exception to serialize
   * @return a map representing the JSON error structure
   */
  public Map<String, Object> serialize(String errorCode, Exception exception) {
    return serialize(errorCode, exception.getMessage());
  }
}
