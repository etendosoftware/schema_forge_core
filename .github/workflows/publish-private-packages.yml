name: Publish Private Packages

on:
  workflow_dispatch:
    inputs:
      tag:
        description: npm dist-tag to publish
        required: true
        default: alpha
        type: choice
        options:
          - alpha
          - beta
          - latest
      dry_run:
        description: Run npm publish with --dry-run
        required: true
        default: true
        type: boolean

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: Publish GitHub Packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          registry-url: https://npm.pkg.github.com
          scope: '@etendosoftware'

      - run: npm install

      - name: Schema Forge core tests
        run: npm test --workspace=packages/schema-forge-core

      - name: App shell core tests
        run: npm test --workspace=packages/app-shell-core

      - name: App shell core consumer smoke
        run: npm run test:consumer --workspace=packages/app-shell-core

      - name: Schema Forge agent context tests
        run: npm test --workspace=packages/schema-forge-agent-context

      - name: Schema Forge stack tests
        run: npm test --workspace=packages/schema-forge-stack

      - name: Schema Forge CLI tests
        run: npm test --workspace=cli

      - name: Pack packages
        run: |
          npm pack --workspace=packages/schema-forge-core --dry-run
          npm pack --workspace=packages/app-shell-core --dry-run
          npm pack --workspace=packages/schema-forge-agent-context --dry-run
          npm pack --workspace=packages/schema-forge-stack --dry-run
          npm pack --workspace=packages/etendo-go-core --dry-run
          npm pack --workspace=cli --dry-run

      - name: Publish schema forge core
        run: |
          DRY_RUN_ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            DRY_RUN_ARGS="--dry-run"
          fi
          NAME=$(node -p "require('./packages/schema-forge-core/package.json').name")
          VERSION=$(node -p "require('./packages/schema-forge-core/package.json').version")
          if npm view "$NAME@$VERSION" version >/dev/null 2>&1; then
            echo "$NAME@$VERSION is already published — skipping (bump the version to publish new changes)."
          else
            npm publish --workspace=packages/schema-forge-core --tag "${{ inputs.tag }}" $DRY_RUN_ARGS
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish app shell core
        run: |
          DRY_RUN_ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            DRY_RUN_ARGS="--dry-run"
          fi
          NAME=$(node -p "require('./packages/app-shell-core/package.json').name")
          VERSION=$(node -p "require('./packages/app-shell-core/package.json').version")
          if npm view "$NAME@$VERSION" version >/dev/null 2>&1; then
            echo "$NAME@$VERSION is already published — skipping (bump the version to publish new changes)."
          else
            npm publish --workspace=packages/app-shell-core --tag "${{ inputs.tag }}" $DRY_RUN_ARGS
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish schema forge agent context
        run: |
          DRY_RUN_ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            DRY_RUN_ARGS="--dry-run"
          fi
          NAME=$(node -p "require('./packages/schema-forge-agent-context/package.json').name")
          VERSION=$(node -p "require('./packages/schema-forge-agent-context/package.json').version")
          if npm view "$NAME@$VERSION" version >/dev/null 2>&1; then
            echo "$NAME@$VERSION is already published — skipping (bump the version to publish new changes)."
          else
            npm publish --workspace=packages/schema-forge-agent-context --tag "${{ inputs.tag }}" $DRY_RUN_ARGS
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish schema forge stack
        run: |
          DRY_RUN_ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            DRY_RUN_ARGS="--dry-run"
          fi
          NAME=$(node -p "require('./packages/schema-forge-stack/package.json').name")
          VERSION=$(node -p "require('./packages/schema-forge-stack/package.json').version")
          if npm view "$NAME@$VERSION" version >/dev/null 2>&1; then
            echo "$NAME@$VERSION is already published — skipping (bump the version to publish new changes)."
          else
            npm publish --workspace=packages/schema-forge-stack --tag "${{ inputs.tag }}" $DRY_RUN_ARGS
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish etendo-go-core
        run: |
          DRY_RUN_ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            DRY_RUN_ARGS="--dry-run"
          fi
          NAME=$(node -p "require('./packages/etendo-go-core/package.json').name")
          VERSION=$(node -p "require('./packages/etendo-go-core/package.json').version")
          if npm view "$NAME@$VERSION" version >/dev/null 2>&1; then
            echo "$NAME@$VERSION is already published — skipping (bump the version to publish new changes)."
          else
            npm publish --workspace=packages/etendo-go-core --tag "${{ inputs.tag }}" $DRY_RUN_ARGS
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish schema forge CLI
        run: |
          DRY_RUN_ARGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            DRY_RUN_ARGS="--dry-run"
          fi
          NAME=$(node -p "require('./cli/package.json').name")
          VERSION=$(node -p "require('./cli/package.json').version")
          if npm view "$NAME@$VERSION" version >/dev/null 2>&1; then
            echo "$NAME@$VERSION is already published — skipping (bump the version to publish new changes)."
          else
            npm publish --workspace=cli --tag "${{ inputs.tag }}" $DRY_RUN_ARGS
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
